<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-2xl bg-white rounded-xl shadow-xl p-8">
        
        <h2 class="text-3xl font-bold text-center text-blue-900 mb-8"><%= title %></h2>

        <form action="<%= treino ? '/treinos/editar/'+treino.id : '/treinos/criar' %>" method="POST" class="space-y-6">
            
            <div>
                <label class="block text-gray-700 font-bold mb-2">Nome do Treino</label>
                <input type="text" name="nome" value="<%= treino ? treino.nome : '' %>" required
                    class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition shadow-sm"
                    placeholder="Ex: Treino A - Hipertrofia">
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Aquecimento</label>
                    <span class="text-xs text-gray-500 block mb-2">(Um item por linha)</span>
                    <textarea name="aquecimento" rows="5"
                        class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition shadow-sm placeholder-gray-400"
                        placeholder="Ex:&#10;Esteira 10 min&#10;Alongamento dinâmico"><%= treino ? treino.aquecimento : '' %></textarea>
                </div>

                <div>
                    <label class="block text-gray-700 font-bold mb-2">Exercícios</label>
                    <span class="text-xs text-gray-500 block mb-2">(Um exercício por linha)</span>
                    <textarea name="exercicios" rows="5" required
                        class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition shadow-sm placeholder-gray-400"
                        placeholder="Ex:&#10;Supino Reto - 3x10&#10;Crossfire - 4x12&#10;Tríceps Corda - 3x15"><%= treino ? treino.exercicios : '' %></textarea>
                </div>
            </div>

            <div>
                <label class="block text-gray-700 font-bold mb-2">Tempo de Descanso</label>
                <input type="text" name="descanso" value="<%= treino ? treino.descanso : '' %>"
                    class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 transition shadow-sm"
                    placeholder="Ex: 60-90 segundos">
            </div>

            <div class="flex gap-4 pt-4">
                <a href="/treinos" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 rounded-lg text-center transition shadow-md">
                    Cancelar
                </a>
                <button type="submit" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-lg shadow-lg hover:shadow-xl transition transform hover:-translate-y-1">
                    Salvar Treino
                </button>
            </div>

        </form>
    </div>

</body>
</html>