<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professores</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .card { transition: all 0.2s ease; }
        .card:hover {
            transform: scale(1.05);
            background-color: rgba(255, 255, 255, 0.9);
            box-shadow: 0 20px 40px rgba(156, 170, 233, 0.651);
        }
    </style>
</head>

<body class="bg-cover bg-center bg-fixed" style="background-image: url('/images/1000209218.png'); background-color: #f0f4f8;">
    
    <a href="/" class="absolute top-4 left-4 text-3xl font-bold text-black no-underline bg-white/50 px-3 rounded-lg hover:bg-white/80 transition">&#x2190;</a>

    <div class="py-8 flex flex-col items-center">
        <h1 class="text-3xl font-bold text-center text-black drop-shadow-md bg-white/60 px-6 py-2 rounded-xl mb-6">PROFESSORES</h1>
        
        <a href="/professores/criar" class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-6 rounded-lg shadow-lg transition transform hover:-translate-y-1">
            + Novo Professor
        </a>
    </div>

    <div class="flex flex-wrap justify-center gap-8 pb-10">
        
        <% if(professores.length === 0) { %>
            <div class="bg-white p-6 rounded-lg shadow-md text-center">Nenhum professor cadastrado.</div>
        <% } %>

        <% professores.forEach(function(prof) { %>
        <div class="card flex flex-col sm:flex-row items-center w-[90%] sm:w-[60%] md:w-[45%] lg:w-[30%] xl:w-[24%] bg-blue-200/80 backdrop-blur-sm rounded-lg shadow-md p-4 relative group">
            
            <img src="/images/<%= prof.foto %>" onerror="this.src='https://via.placeholder.com/150?text=Prof';" class="w-32 h-32 rounded-lg object-cover border-2 border-white shadow-sm">
            
            <div class="ml-0 sm:ml-4 mt-4 sm:mt-0 text-center sm:text-left flex-1">
                <div class="text-lg text-gray-800">
                    <strong class="block text-xl text-black mb-1"><%= prof.nome %></strong>
                    <span class="text-sm">Professor(a) de</span><br>
                    <strong class="text-blue-900"><%= prof.disciplina %></strong>
                </div>
            </div>

            <div class="absolute top-2 right-2 flex gap-2 opacity-100 sm:opacity-0 group-hover:opacity-100 transition-opacity">
                <a href="/professores/editar/<%= prof.id %>" class="bg-yellow-400 hover:bg-yellow-500 text-black p-1.5 rounded text-xs font-bold shadow" title="Editar">✏️</a>
                <a href="/professores/deletar/<%= prof.id %>" onclick="return confirm('Excluir <%= prof.nome %>?')" class="bg-red-500 hover:bg-red-600 text-white p-1.5 rounded text-xs font-bold shadow" title="Excluir">🗑️</a>
            </div>

        </div>
        <% }); %>

    </div>

</body>
</html>