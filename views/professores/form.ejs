<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-cover bg-center min-h-screen flex items-center justify-center p-4" style="background-image: url('/images/1000209218.png'); background-color: #f0f4f8;">

    <div class="w-full max-w-lg bg-white/90 backdrop-blur-md rounded-2xl shadow-2xl p-8 transform transition-all">
        
        <h2 class="text-3xl font-bold text-center text-blue-900 mb-8"><%= title %></h2>

        <form action="<%= professor ? '/professores/editar/'+professor.id : '/professores/criar' %>" method="POST" class="space-y-6">
            
            <div>
                <label class="block text-gray-700 font-bold mb-2">Nome do Professor</label>
                <input type="text" name="nome" value="<%= professor ? professor.nome : '' %>" required
                    class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition shadow-sm placeholder-gray-400"
                    placeholder="Ex: Jurandy Martins">
            </div>

            <div>
                <label class="block text-gray-700 font-bold mb-2">Disciplina</label>
                <input type="text" name="disciplina" value="<%= professor ? professor.disciplina : '' %>" required
                    class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition shadow-sm placeholder-gray-400"
                    placeholder="Ex: Redes de Computadores">
            </div>

            <div>
                <label class="block text-gray-700 font-bold mb-2">Nome do Arquivo da Foto</label>
                <div class="flex gap-2">
                    <input type="text" name="foto" value="<%= professor ? professor.foto : '' %>"
                        class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition shadow-sm placeholder-gray-400"
                        placeholder="Ex: juju.png">
                </div>
                <p class="text-xs text-gray-500 mt-1">Certifique-se que a imagem está na pasta public/images/</p>
            </div>

            <div class="flex gap-4 pt-4">
                <a href="/professores" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 rounded-lg text-center transition shadow-md">
                    Cancelar
                </a>
                <button type="submit" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-lg shadow-lg hover:shadow-xl transition transform hover:-translate-y-1">
                    Salvar
                </button>
            </div>

        </form>
    </div>

</body>
</html>