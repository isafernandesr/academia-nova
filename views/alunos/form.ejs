<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  
  <style>
      body { 
          font-family: 'Poppins', sans-serif;
          background-color: #f0f4f8; 
          background-image: url('/images/1000209218.png'); /* Sua imagem de fundo */
          background-size: cover;
          background-position: center;
          min-height: 100vh;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 20px;
      }

      .card-custom { 
          background: rgba(255, 255, 255, 0.95); /* Efeito vidro */
          border-radius: 20px;
          box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
          backdrop-filter: blur(10px);
          max-width: 700px;
          width: 100%;
          border: none;
          padding: 30px;
      }

      h2 {
          color: #37499b;
          font-weight: 600;
          text-align: center;
          margin-bottom: 25px;
      }

      .form-label {
          font-weight: 600;
          color: #555;
      }

      .form-control {
          border-radius: 10px;
          border: 1px solid #ccc;
          padding: 10px;
      }

      .form-control:focus {
          border-color: #6fa6e9;
          box-shadow: 0 0 0 0.2rem rgba(111, 166, 233, 0.25);
      }

      /* Botão estilizado igual ao seu index */
      .btn-tech {
          background: linear-gradient(135deg, #5c91c4, #6fa6e9);
          border: none;
          color: white;
          font-weight: bold;
          padding: 12px;
          border-radius: 10px;
          transition: transform 0.2s, box-shadow 0.2s;
          width: 100%;
      }

      .btn-tech:hover {
          background: linear-gradient(135deg, #6ba4eb, #00c6ff);
          transform: translateY(-2px);
          box-shadow: 0 4px 12px rgba(100, 165, 245, 0.4);
          color: white;
      }

      .btn-voltar {
          background-color: #6c757d;
          color: white;
          border-radius: 10px;
          width: 100%;
          display: block;
          text-align: center;
          padding: 12px;
          text-decoration: none;
          font-weight: bold;
          border: none;
      }
      .btn-voltar:hover {
          background-color: #5a6268;
          color: white;
      }
  </style>
</head>
<body>

  <div class="card-custom">
      <h2><%= title %></h2>

    <% if (typeof erros !== 'undefined' && erros.length > 0) { %>
        <div class="alert alert-danger" role="alert" style="background-color: #f8d7da; color: #721c24; padding: 10px; border-radius: 10px; margin-bottom: 20px; border: 1px solid #f5c6cb;">
            <ul style="margin: 0; padding-left: 20px;">
                <% erros.forEach(function(erro) { %>
                    <li><%= erro %></li>
                <% }); %>
            </ul>
        </div>
      <% } %>
      

      
      <form action="<%= aluno ? '/alunos/editar/'+aluno.id : '/alunos/criar' %>" method="POST">
        
        <div class="mb-3">
          <label class="form-label">Nome Completo</label>
          <input type="text" name="nome" class="form-control" value="<%= aluno ? aluno.nome : '' %>" required placeholder="Ex: João da Silva">
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label class="form-label">Idade</label>
                <input type="number" name="idade" class="form-control" value="<%= aluno ? aluno.idade : '' %>" required placeholder="Ex: 18">
            </div>
            <div class="col-md-6 mb-3">
                <label class="form-label">Aniversário</label>
                <input type="text" name="aniversario" class="form-control" placeholder="DD/MM/AAAA" value="<%= aluno ? aluno.aniversario : '' %>" required>
            </div>
        </div>

        <div class="mb-3">
          <label class="form-label">Gosta de...</label>
          <textarea name="gosta" class="form-control" rows="2" placeholder="Ex: Jogar vôlei e ler"><%= aluno ? aluno.gosta : '' %></textarea>
        </div>

        <div class="mb-3">
          <label class="form-label">Sonho</label>
          <textarea name="sonho" class="form-control" rows="2" placeholder="Ex: Viajar o mundo"><%= aluno ? aluno.sonho : '' %></textarea>
        </div>

        <div class="row mt-4">
            <div class="col-6">
                <a href="/alunos" class="btn-voltar">Cancelar</a>
            </div>
            <div class="col-6">
                <button class="btn-tech" type="submit">Salvar</button>
            </div>
        </div>

      </form>
  </div>

</body>
</html>